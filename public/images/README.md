# Медиа BERG STUDIO

Фотографии временно сняты с сайта. Во всех визуальных блоках отображаются
редакционные плейсхолдеры «Нет фото» (компонент `SiteImage`).

Когда появятся утверждённые снимки студии — добавьте файлы сюда и верните
`src` в `SiteImage` / пути в секциях.
