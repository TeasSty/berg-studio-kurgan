---
import { site } from '../data/site';
---

<section class="intro section section--ivory" id="intro" aria-labelledby="intro-title">
  <div class="container intro__inner">
    <div class="intro__content">
      <h2 class="section-title reveal" id="intro-title">
        О&nbsp;студии
      </h2>
      <p class="intro__text reveal reveal-delay-1">
        Добро пожаловать в студию косметологии и красоты Berg studio.
        Курган, 2-й микрорайон, 8.
      </p>
      <p class="intro__text reveal reveal-delay-2">
        Дипломированный косметолог с медицинским образованием и действующей
        аккредитацией. Лауреат премии Топ-100 успешных людей Курганской
        области. После посещения остаёмся на связи по всем вопросам.
      </p>
    </div>

    <aside class="intro__aside reveal reveal-delay-2" aria-label="Принцип работы">
      <div class="editorial-panel editorial-panel--quote">
        <span class="editorial-panel__quote-mark" aria-hidden="true">«</span>
        <p class="editorial-panel__quote-text">Не навреди</p>
        <p class="editorial-panel__quote-author">{site.name}</p>
      </div>
    </aside>
  </div>
</section>

<style>
  .intro__inner {
    display: grid;
    gap: var(--space-xl);
    align-items: center;
  }

  .intro__text {
    margin-top: var(--space-md);
    max-width: 48ch;
    color: var(--color-text-dark-muted);
    font-size: 1.0625rem;
  }

  @media (min-width: 1024px) {
    .intro__inner {
      grid-template-columns: 1.1fr 0.9fr;
      gap: var(--space-3xl);
    }

    .intro__aside {
      justify-self: end;
    }
  }
</style>
