---
import { site } from '../data/site';
---

<section class="intro section section--ivory" id="intro" aria-labelledby="intro-title">
  <div class="container intro__inner">
    <div class="intro__content">
      <h2 class="section-title reveal" id="intro-title">
        О&nbsp;студии
      </h2>
      <p class="intro__text reveal reveal-delay-1">
        BERG STUDIO — мой кабинет в Кургане, 2-й микрорайон, 8. Работаю
        с инъекциями, аппаратными методиками и уходом за кожей.
      </p>
      <p class="intro__text reveal reveal-delay-2">
        Пришла из режимной медицины — поэтому для меня важны стерильность,
        аккуратность и честный разговор до процедуры. Не предлагаю лишнего:
        сначала смотрю, потом предлагаю план.
      </p>
      <p class="intro__signature reveal reveal-delay-3">
        — Жанна
      </p>
    </div>

    <aside class="intro__aside reveal reveal-delay-2" aria-label="Принцип работы">
      <div class="editorial-panel editorial-panel--quote">
        <span class="editorial-panel__quote-mark" aria-hidden="true">«</span>
        <p class="editorial-panel__quote-text">Не навреди — и&nbsp;сделай красиво</p>
        <p class="editorial-panel__quote-author">Жанна · {site.name}</p>
      </div>
    </aside>
  </div>
</section>

<style>
  .intro__inner {
    display: grid;
    gap: var(--space-xl);
    align-items: center;
  }

  .intro__text {
    margin-top: var(--space-md);
    max-width: 48ch;
    color: var(--color-text-dark-muted);
    font-size: 1.0625rem;
  }

  .intro__signature {
    margin-top: var(--space-lg);
    font-family: var(--font-display);
    font-size: 1.125rem;
    font-style: italic;
    color: var(--color-taupe);
  }

  @media (min-width: 1024px) {
    .intro__inner {
      grid-template-columns: 1.1fr 0.9fr;
      gap: var(--space-3xl);
    }

    .intro__aside {
      justify-self: end;
    }
  }
</style>
